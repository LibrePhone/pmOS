#!/sbin/openrc-run

name=
command="/usr/bin/$name"
command_args="$sample_opts"
command_background="yes"

start_stop_daemon_args="container-manager --daemon --data-path=/var/lib/anbox"
pidfile="/run/$name.pid"

depend() {
	need net
	after firewall
}
